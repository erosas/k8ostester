# Baseline: reads hit the primary too (no read offload). The primary is the
# bottleneck and reads blip during failover.
extends: ../_scenario.yaml
name: read-scaling-primary-only
config: {manifests: ../_cluster}
load:
  endpoint_ro: pg-rw
