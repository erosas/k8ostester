apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8ost-console.fullname" . }}
  labels: {{- include "k8ost-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "k8ost-console.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
