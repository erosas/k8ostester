{
  "type": "timeseries",
  "title": "WAL archive lag",
  "description": "Seconds since the last WAL file was successfully archived to object storage. This is the health signal: low and flat = archiving is keeping up; a steady climb = archiving has stalled, so the PITR window stops advancing and WAL piles up on the primary. The waterline, if set, is your max-delay goal.",
  "datasource": {"type": "prometheus", "uid": "${datasource}"},
  "targets": [
    {"expr": "time() - cnpg_pg_stat_archiver_last_archived_time{__LABEL__=~\"__CLUSTER__-[0-9]+\"}", "legendFormat": "since last archive", "refId": "A"}
  ],
  "fieldConfig": {
    "defaults": {"unit": "s", "custom": {"drawStyle": "line", "fillOpacity": 10}, "color": {"mode": "palette-classic"}},
    "overrides": []
  }
}
