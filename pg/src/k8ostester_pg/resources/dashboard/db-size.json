{
  "type": "timeseries",
  "title": "Database size",
  "description": "On-disk size of each database (templates excluded). Every instance reports the same replicated data, so this is de-duplicated with max() — one line per database, not per pod.",
  "datasource": {"type": "prometheus", "uid": "${datasource}"},
  "targets": [
    {"expr": "max by (datname) (cnpg_pg_database_size_bytes{__LABEL__=~\"__CLUSTER__-[0-9]+\", datname!~\"template.*\"})", "legendFormat": "{{datname}}", "refId": "A"}
  ],
  "fieldConfig": {
    "defaults": {"unit": "bytes", "custom": {"drawStyle": "line", "fillOpacity": 12}, "color": {"mode": "palette-classic"}},
    "overrides": []
  }
}
