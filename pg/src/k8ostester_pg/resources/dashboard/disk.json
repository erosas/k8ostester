{
  "type": "timeseries",
  "title": "Disk usage (% of volume)",
  "description": "Per-instance data-volume fill, from the kubelet (kubelet_volume_stats_*). Needs a Prometheus scraping the kubelet — some clusters (e.g. plain kind) don't expose it. Storage is grow-only, so watch this and expand before it fills. The waterline, if set, is your max-disk goal.",
  "datasource": {"type": "prometheus", "uid": "${datasource}"},
  "targets": [
    {"expr": "kubelet_volume_stats_used_bytes{persistentvolumeclaim=~\"__CLUSTER__-[0-9]+\"} / kubelet_volume_stats_capacity_bytes{persistentvolumeclaim=~\"__CLUSTER__-[0-9]+\"} * 100", "legendFormat": "{{persistentvolumeclaim}}", "refId": "A"}
  ],
  "fieldConfig": {
    "defaults": {"unit": "percent", "min": 0, "max": 100, "custom": {"drawStyle": "line", "fillOpacity": 12}, "color": {"mode": "palette-classic"}},
    "overrides": []
  }
}
