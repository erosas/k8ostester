{
  "type": "timeseries",
  "title": "Replication & slots",
  "description": "Standbys actively streaming from the primary, and the number of active replication slots. If active slots drop below the number of replicas, an inactive slot is pinning WAL — which can fill the primary's disk.",
  "datasource": {"type": "prometheus", "uid": "${datasource}"},
  "targets": [
    {"expr": "max(cnpg_pg_replication_streaming_replicas{__LABEL__=~\"__CLUSTER__-[0-9]+\"})", "legendFormat": "streaming replicas", "refId": "A"},
    {"expr": "max(cnpg_pg_replication_slots_active{__LABEL__=~\"__CLUSTER__-[0-9]+\"})", "legendFormat": "active slots", "refId": "B"}
  ],
  "fieldConfig": {
    "defaults": {"unit": "short", "custom": {"drawStyle": "line", "fillOpacity": 8}, "color": {"mode": "palette-classic"}},
    "overrides": []
  }
}
